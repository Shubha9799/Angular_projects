import { TxtbackgroundDirective } from './txtbackground.directive';

describe('TxtbackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new TxtbackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
